<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Activa tu Cuenta</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #4CAF50; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background-color: #f9f9f9; }
        .activation-box { background-color: white; padding: 20px; margin: 20px 0; border-radius: 5px; border-left: 4px solid #4CAF50; text-align: center; }
        .activation-button { 
            display: inline-block; 
            background-color: #4CAF50; 
            color: white; 
            padding: 15px 30px; 
            text-decoration: none; 
            border-radius: 5px; 
            font-weight: bold;
            margin: 20px 0;
        }
        .activation-button:hover { background-color: #45a049; }
        .footer { text-align: center; padding: 20px; color: #666; }
        .welcome-icon { font-size: 48px; margin-bottom: 10px; }
        .warning { background-color: #fff3cd; padding: 15px; border-radius: 5px; margin: 15px 0; }
        .link-text { word-break: break-all; background-color: #f5f5f5; padding: 10px; border-radius: 3px; font-family: monospace; }
    </style>
</head>
<body>
    <div class="header">
        <div class="welcome-icon">🎉</div>
        <h1>¡Bienvenido a nuestra plataforma!</h1>
    </div>
    
    <div class="content">
        <p>Hola <strong>{{name}}</strong>,</p>
        
        <p>¡Gracias por registrarte! Para completar tu registro y comenzar a usar tu cuenta, necesitas activarla haciendo clic en el botón de abajo:</p>
        
        <div class="activation-box">
            <h3>🔐 Activar mi cuenta</h3>
            <a href="{{activationUrl}}" class="activation-button">ACTIVAR CUENTA</a>
            
            <p>Si el botón no funciona, copia y pega este enlace en tu navegador:</p>
            <div class="link-text">{{activationUrl}}</div>
        </div>
        
        <div class="warning">
            <strong>⏰ Importante:</strong> Este enlace de activación expirará en 24 horas por seguridad. Si no activas tu cuenta en este tiempo, deberás solicitar un nuevo enlace de activación.
        </div>
        
        <p>Si no te registraste en nuestra plataforma, puedes ignorar este email.</p>
    </div>
    
    <div class="footer">
        <p>Fecha de registro: {{date}}</p>
        <p>Este es un mensaje automático, por favor no responder a este email.</p>
    </div>
</body>
</html>